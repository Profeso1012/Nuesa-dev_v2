First task, I want you to go through all the placeholder images in the carousels, events gallery in the home, about, each departments, and events pages, and do this:

1. Make sure you replace any image related to person with the #person.png in the public folder, and any related to events with image.png.

2. In all the pages, the number of divs displayed at a time carousels appear three at a time (besides the special image gallery and latest carousel whcih has a large image untop, then 4 under). There should be placeholder text in all fields of the div, not just no infomation or content if the backend returns an empty data, so all three divs have the placeholder images, and show the styling for how the texts (name of person, event date, bio, etc will look like when there is data.)

3. The motion of the carousel should be from left to right fro ever, and they are currently moving all three at the same time, intead of one by one i.e the leftmost (1st) moves out, the second becomes 1st, and third becomes second, then a 4th comes in as if from the right and takes the psition of the 3rd, then the 5th... until if circles back to the 1st which still in from the right as if a xth div). Same thing with the events and latest news carosel. The first data (image, event name, news text) is rendered into the large div untop, then the second into the first div under with black border, then the third, 4th and 5th into the remaining thinner orange bordered divs (ony the large div shows the text associated), then th 6th, etc wait to come in, which as it doesm the seocnd data renders into the first (larger div), the 3rd to the second div (the blak bordered div), the 4th into the first orange bordered div...So the divs dont move, its the data in them that changes.